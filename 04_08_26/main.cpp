#include "twentyFourHrClock.h"

int main()
{
    int x;
    int y = 6;
    int z = 10;
    x = 4 + (z + y);
    TwentyFourHrClock clock;
    clock = clock + 10 + 10;
    // clock = 10 + clock;
    return 0;
}